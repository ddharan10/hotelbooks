
pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {

                git branch: 'main',url: 'https://github.com/ddharan10/hotelbook.git'
            }
        }

        stage('Run Cucumber Tests') {
            steps {
                bat "mvn clean test"
            }
        }
    }
}
